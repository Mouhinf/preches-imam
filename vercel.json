{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "pip install --break-system-packages -r requirements.txt && cd frontend && npm install",
  "functions": {
    "api/**/*.py": {
      "maxDuration": 60
    }
  }
}